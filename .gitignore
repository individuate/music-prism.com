# Working context, synced from the private app repo — deliberately NOT tracked.
#
# This repo is public and serves music-prism.com. Product strategy, design
# rationale, and agent working instructions are backstage: they belong in the
# private app repo, which is their canonical home. These copies exist only so
# tooling resolves them from this root. Re-sync with the command in CLAUDE.md.
PRODUCT.md
DESIGN.md
CLAUDE.md
.impeccable/

.DS_Store
